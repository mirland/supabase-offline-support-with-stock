name: supabase_offline_support_with_stock
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  equatable: ^2.0.0
  supabase_flutter: ^0.3.3
  async: ^2.8.2
  floor: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.2
  floor_generator: ^1.3.0
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

